# REMOVED: Secrets previously stored here were removed to avoid exposing credentials.
# If you need local development secrets, keep them in a local file (e.g., .env.local) and do NOT commit them.
# Rotate any credentials that were committed.
